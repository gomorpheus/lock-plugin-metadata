---
- plugin:
    category: tab
    description: Shows a tab, which reads from a database table and allows delete operations via controller
    url: https://github.com/spoonboy-io/custom-tab-plugin.git
    tags: database,starter,controller,ui
    minimumMorpheus: 6.0.0
    versioning:
        semantic: true
        morpheus: true

- plugin:
    category: tab
    description: Shows a configurable iframe in a tab, iframe src, iframe height, tab title and tab heading can be configured
    url: https://github.com/spoonboy-io/iframe-tab-plugin.git
    tags: ui,iframe,tab
    minimumMorpheus: 6.1.1
    versioning:
        semantic: true
        morpheus: true
